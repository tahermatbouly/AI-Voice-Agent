# AI-Voice-Agent.
An AI voice agent that answers company calls, converses naturally in Egyptian Arabic, extracts caller details (name, address, position, inquiry) in real time, replies with natural speech, detects when the call ends, and saves a structured record plus transcript for every call.
